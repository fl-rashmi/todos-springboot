spring:
  application:
    name: todo

  data:
    mongodb:
      host: localhost
      port: 27017
      database: todo
      auto-index-creation: true

server:
  port: 3000
  servlet:
    context-path: /
